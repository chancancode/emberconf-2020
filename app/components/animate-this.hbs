<svg width={{this.width}} height={{this.height}} {{on "mousemove" this.onMouseMove}}>
  {{#each this.children as |n|}}
    <circle cx={{n.x}} cy={{n.y}} r={{n.radius}} fill={{n.color}} />
  {{/each}}
</svg>
